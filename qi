#!/usr/bin/env bash
ROOT="$(cd "$(dirname "$0")" && pwd)"
VENV="${QI_VENV:-$ROOT/.venv}"
if [ -x "$VENV/bin/qi" ]; then
  exec "$VENV/bin/qi" "$@"
fi
echo "Venv not ready. Run ./scripts/bootstrap.sh local first."
exit 1
